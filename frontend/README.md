# Frontend

React-based document management UI. Planned for after the backend API is stable.

- React + TypeScript
- Communicates with the backend REST API at `/api`
- Document upload, list, metadata view, content reading
